{
  "$comment": "Plan mode config. Copy to .pi/plan-mode.json (project) or ~/.pi/agent/plan-mode.json (global). Everything is optional.",
  "reasoningEffort": "low",
  "profiles": {
    "julia": {
      "description": "Planning with Julia script execution",
      "bash": ["julia"],
      "tools": ["kaimon*"]
    }
  }
}
